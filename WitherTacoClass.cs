using System;

namespace ClickerAddon
{
	public class WitherTacoClass
	{
		public static class Math
		{
			/*public static float Radius()
			{
				float fluct = 1f + (float)Math.Sin(2 * Math.PI * (Main.GameUpdateCount % 60) / 60f);
				return (fluct / 2);
			}*/
		}
	}
}